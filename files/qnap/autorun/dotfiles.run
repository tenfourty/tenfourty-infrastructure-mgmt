#!/bin/sh

DOTFILESDIR=/share/homes/admin/tenfourty-infrastructure-mgmt/files/qnap/dotfiles

for i in `ls ${DOTFILESDIR}/.*`; do echo "linking /root/$i to $DOTFILESDIR/$i"; (ln -s $DOTFILESDIR/$i /root/$i) ; done
